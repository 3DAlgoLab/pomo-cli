[project]
name = "pomo"
version = "0.1.5"
description = "SSH-friendly CLI pomodoro timer"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
pomo = "pomo.cli:main"

[dependency-groups]
dev = ["pytest>=8.0", "ruff>=0.15.12"]
